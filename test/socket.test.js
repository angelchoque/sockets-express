import { createServer } from 'http';
import { Server } from "socket.io";
import Client from 'socket.io-client'

describe("testing socket.io", () => {
    // antes de iniciar, creamos el servidor
    beforeAll()
})